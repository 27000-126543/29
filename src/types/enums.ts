export enum WaterAreaType {
  LAKE = 'lake',
  RIVER = 'river',
  OCEAN = 'ocean',
  GUILD = 'guild',
}

export enum WeatherType {
  SUNNY = 'sunny',
  CLOUDY = 'cloudy',
  RAINY = 'rainy',
  STORMY = 'stormy',
  FOGGY = 'foggy',
  SNOWY = 'snowy',
}

export enum TimeOfDay {
  DAWN = 'dawn',
  MORNING = 'morning',
  NOON = 'noon',
  AFTERNOON = 'afternoon',
  DUSK = 'dusk',
  NIGHT = 'night',
}

export enum FishRarity {
  COMMON = 'common',
  UNCOMMON = 'uncommon',
  RARE = 'rare',
  EPIC = 'epic',
  LEGENDARY = 'legendary',
}

export enum ApprovalStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
}

export enum TournamentStatus {
  UPCOMING = 'upcoming',
  REGISTRATION = 'registration',
  IN_PROGRESS = 'in_progress',
  FINISHED = 'finished',
  CANCELLED = 'cancelled',
}

export enum SkillType {
  LURE_FISH = 'lure_fish',
  FAST_REEL = 'fast_reel',
  INSTANT_CATCH = 'instant_catch',
  RARE_BOOST = 'rare_boost',
  WEIGHT_BOOST = 'weight_boost',
}

export enum GuildRole {
  MEMBER = 'member',
  OFFICER = 'officer',
  ADMIN = 'admin',
  LEADER = 'leader',
}

export enum ListingStatus {
  ACTIVE = 'active',
  SOLD = 'sold',
  EXPIRED = 'expired',
  CANCELLED = 'cancelled',
}
